//#include "csapp.h"
#include <stdio.h>
#include <stdlib.h>


int main(int cmd)
{
/*  int fd1, fd2;
  char c;

  fd1 = Open("foobar.txt", O_RDONLY, 0);
  fd2 = Open("foobar.txt", )_RDONLY, 0);
  Read(fd1, &c, 1);
  Read(fd2, &c, 1);
  printf("c = %c\n", c);
*/
  printf("Your command line argument is %d\n", cmd);
  exit(0);
}
